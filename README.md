# RoboNav Test Backend

Веб-интерфейс для мониторинга и управления роботом через ROS2/rosbridge. Приложение позволяет в реальном времени просматривать данные с камеры, карту окружения, позицию робота, а также управлять роботом и отправлять команды.

## Возможности

- 📹 **Просмотр видео с камеры** - отображение изображений из ROS топиков в реальном времени
- 🗺️ **Интерактивная карта** - визуализация occupancy grid с возможностью масштабирования и панорамирования
- 🤖 **Отображение робота** - показ текущей позиции и ориентации робота на карте
- 📍 **Добавление точек** - клик по карте для добавления координат с возможностью редактирования
- 🎯 **Отправка команд** - публикация координат в ROS2 топик `/args_map`
- 🛑 **Экстренная остановка** - кнопка для немедленной остановки робота
- 📊 **Мониторинг статуса** - отображение статуса выполнения задач роботом

## Требования

- Python 3.8+
- ROS2 (на роботе)
- rosbridge_suite (на роботе)
- Доступ к роботу в локальной сети

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd robonav-test-backend
```

2. Установите зависимости:
```bash
pip3 install -r requirements-minimal.txt
```

**Примечание:** Файл `requirements.txt` содержит ROS-специфичные пакеты, которые не устанавливаются через pip. Используйте `requirements-minimal.txt` для установки только необходимых зависимостей веб-приложения.

## Настройка

Откройте файл `app.py` и измените параметры подключения к роботу:

```python
ROBOT_IP = '192.168.0.137'  # IP-адрес робота в вашей сети
ROSBRIDGE_PORT = 9090        # Порт rosbridge (по умолчанию 9090)
```

## Запуск

Запустите сервер FastAPI:

```bash
python3 app.py
```

Или используйте uvicorn напрямую:

```bash
uvicorn app:app --host 0.0.0.0 --port 8000
```

После запуска откройте в браузере:
```
http://localhost:8000
```

## Использование

### Просмотр топиков

На главной странице отображается список всех доступных ROS топиков, полученных с робота через rosbridge.

### Работа с камерой

1. Перейдите на вкладку "Камера и Карта"
2. Выберите топик камеры из выпадающего списка (например, `/camera/image_raw`)
3. Нажмите "Подписаться" для начала получения изображений
4. Изображение будет отображаться в реальном времени

### Работа с картой

1. Нажмите "Подписаться" для получения карты из топика `/map`
2. Карта отобразится с системой координат
3. **Масштабирование**: прокрутите колесико мыши на карте
4. **Панорамирование**: зажмите колесико мыши и перетащите карту
5. **Добавление точки**: кликните левой кнопкой мыши на карте
6. **Редактирование координат**: измените значения X, Y или Угол в таблице
7. **Отправка точек**: нажмите "Отправить в args_map" для публикации координат в ROS2

### Управление роботом

- **Экстренная остановка**: кнопка "СТОП" отправляет команду остановки в топик `/cmd_vel`
- **Статус задания**: автоматически отображается статус выполнения задач из топика `/task_status`
- **Очистка пути**: кнопка "Очистить путь" удаляет историю движения робота с карты

## Структура проекта

```
robonav-test-backend/
├── app.py                      # Основной файл FastAPI приложения
├── requirements-minimal.txt    # Минимальные зависимости (для установки)
├── requirements.txt            # Полный список зависимостей (включая ROS)
├── templates/
│   └── index.html             # Веб-интерфейс (HTML, CSS, JavaScript)
├── CHECK_TOPIC.md             # Инструкции по проверке ROS топиков
└── README.md                  # Этот файл
```

## ROS2 Топики

Приложение работает со следующими топиками:

- `/map` - карта окружения (nav_msgs/OccupancyGrid)
- `/camera/image_raw` или `/camera/image_raw/compressed` - изображение с камеры
- `/lio_sam/mapping/odometry` - одометрия робота (nav_msgs/Odometry)
- `/task_status` - статус выполнения задач
- `/cmd_vel` - команды управления роботом (geometry_msgs/Twist)
- `/args_map` - координаты точек для навигации (std_msgs/Float64MultiArray)

## Проверка топиков

Для проверки работы топиков используйте команды ROS2:

```bash
# Список всех топиков
ros2 topic list

# Тип топика
ros2 topic type /args_map

# Просмотр сообщений топика
ros2 topic echo /args_map
```

Подробные инструкции см. в файле `CHECK_TOPIC.md`.

## Особенности реализации

### Координатные системы

Приложение выполняет преобразование координат между:
- **ROS карта**: начало координат в левом нижнем углу, Y растет вверх
- **HTML Canvas**: начало координат в левом верхнем углу, Y растет вниз

### Масштабирование карты

- Минимальный масштаб: 1.0 (начальный размер)
- Максимальный масштаб: 5.0 (приближение в 5 раз)
- Масштабирование происходит относительно точки под курсором

### Формат данных `/args_map`

Топик `/args_map` публикует данные в формате `std_msgs/Float64MultiArray`:
- Каждая точка представлена тремя значениями: [X, Y, Angle]
- X, Y - координаты в метрах
- Angle - угол в градусах

## Устранение неполадок

### Не удается подключиться к роботу

1. Проверьте, что робот находится в той же сети
2. Убедитесь, что rosbridge запущен на роботе:
   ```bash
   ros2 run rosbridge_server rosbridge_websocket
   ```
3. Проверьте IP-адрес и порт в `app.py`

### Карта не отображается

1. Убедитесь, что топик `/map` публикуется:
   ```bash
   ros2 topic echo /map
   ```
2. Проверьте консоль браузера на наличие ошибок (F12)

### Изображение с камеры не отображается

1. Проверьте, что выбран правильный топик камеры
2. Убедитесь, что топик публикуется:
   ```bash
   ros2 topic echo /camera/image_raw
   ```
3. Проверьте формат изображения (поддерживаются `sensor_msgs/Image` и `sensor_msgs/CompressedImage`)

## Разработка

### Зависимости

Основные зависимости:
- `fastapi` - веб-фреймворк
- `uvicorn` - ASGI сервер
- `websocket-client` - клиент WebSocket для подключения к rosbridge
- `jinja2` - шаблонизатор для HTML

### Архитектура

- **Backend (app.py)**: FastAPI сервер, который подключается к rosbridge и получает список топиков
- **Frontend (templates/index.html)**: Веб-интерфейс, который напрямую подключается к rosbridge через WebSocket для получения данных в реальном времени

## Лицензия

[Укажите лицензию]

## Авторы

[Укажите авторов]

